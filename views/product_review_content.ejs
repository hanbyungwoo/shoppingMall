<!DOCTYPE html>
<html>
<%include ./1.ejs %>
    
    <head>
 <title>Bootstrap Example</title>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link rel="stylesheet" href="http://maxcdn.bootstrapcdn.com/bootstrap/3.3.6/css/bootstrap.min.css">
  <script src="https://ajax.googleapis.com/ajax/libs/jquery/1.12.2/jquery.min.js"></script>
  <script src="http://maxcdn.bootstrapcdn.com/bootstrap/3.3.6/js/bootstrap.min.js"></script>
</head>
<body>



<div class="container">
  <table class="table table-hover">


  <h1><%= title %></h1>

    <form action="/product/review_update" method="get">
    
            <input type="hidden" name="purchasenum" value="<%=rows.purchasenum%>"/>
            <tr>
                <td>작성자</td>
                <td><%=rows.memberid%></td>
            </tr>
            <tr>
                <td>제목</td>
                <td><%=rows.title%></td>
            </tr>
            <tr>
                <td>내용</td>
                <td><%=rows.context%></td>
            </tr>
            <tr>
                <td>등록날짜</td>
                <td><%=rows.reviewdate%></td>
            </tr>
            <tr>
                <td colspan="2">
                    <% if(department == "manager") {%>
                        <button type="submit">글 수정</button>
                    <% } else if(rows.memberid != user_id) { %>
                    <% } else { %>
                        <button type="submit">글 수정</button>
                    <% } %>
                    <a href="/product/product_review/<%=rows.purchasenum%>">리스트로 돌아가기</a>
                </td>
            </tr>
        </table>
    </form>
    </div>

    <form action="/product/review_delete" method="get">
        <input type="hidden" name="purchasenum" value="<%=rows.purchasenum%>"/>

        <input type="hidden" name="productnum" value="<%=productnum%>"/>

        <% if(department == "manager") {%>
            <button  type="submit" class="btn btn-default">삭제</button>
        <% } else if(rows.memberid != user_id) { %>
        <% } else { %>
            <button type="submit" class="btn btn-default">삭제</button>
        <% } %>
        
    </form>

</body>
</html>